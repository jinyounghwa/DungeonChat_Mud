# DungeonChat 개발 가이드

## 프로젝트 정보
- **프로젝트명**: DungeonChat Mud Game
- **타입**: 모바일 웹 기반 판타지 텍스트 MUD 게임
- **개발 기간**: 2주
- **버전**: 1.0
- **최종 수정일**: 2024-12-16

---

## 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택](#기술-스택)
3. [시스템 아키텍처](#시스템-아키텍처)
4. [데이터베이스 설계](#데이터베이스-설계)
5. [API 명세](#api-명세)
6. [게임 시스템 설계](#게임-시스템-설계)
7. [AI 통합 가이드](#ai-통합-가이드)
8. [화면 설계](#화면-설계)
9. [개발 일정](#개발-일정)
10. [테스트 가이드](#테스트-가이드)
11. [배포 가이드](#배포-가이드)

---

## 프로젝트 개요

### 제품 설명
CLI 스타일의 레트로 감성을 가진 모바일 웹 기반 텍스트 던전 RPG 머드(MUD) 게임입니다. Qwen 2.5 7B AI 모델을 활용하여 동적인 던전 탐험 경험을 제공합니다.

### 핵심 기능
- 3가지 직업 시스템 (용사, 마법사, 도둑)
- AI 기반 동적 스토리텔링
- 턴제 전투 시스템
- 저장/불러오기 기능
- RAG 기반 컨텍스트 기억
- CLI 스타일 UI/UX

### 화면 구성
1. **채팅 화면**: 메인 게임 플레이 화면 (CLI 스타일)
2. **마이페이지**: 캐릭터 정보, 인벤토리, 전투 기록

---

## 기술 스택

### Frontend
```
- Framework: Next.js 14 (App Router)
- Language: TypeScript
- Styling: Tailwind CSS / Shadcn Create UI
- State Management: Zustand
- Authentication: JWT (로컬 세션)
- HTTP Client: Axios
```

### Backend
```
- Framework: NestJS
- Language: TypeScript
- Database: PostgreSQL
- ORM: Prisma
- Authentication: JWT + bcrypt (비밀번호 해싱)
- AI Integration: Qwen 2.5 7B (vLLM)
- Vector DB: Chroma (RAG)
```

### Infrastructure
```
- Development: Mac Mini M4
- Containerization: Docker Compose
- AI Serving: vLLM
- Deployment: Local Hosting
```

---

## 시스템 아키텍처

### 전체 구조
```
┌─────────────────┐
│   Mobile Web    │
│   (Next.js)     │
└────────┬────────┘
         │ HTTPS
         ↓
┌─────────────────┐
│   API Gateway   │
│   (NestJS)      │
└────────┬────────┘
         │
    ┌────┴────┬──────────┬──────────┐
    ↓         ↓          ↓          ↓
┌────────┐ ┌─────┐ ┌─────────┐ ┌──────┐
│  JWT   │ │ DB  │ │  Qwen   │ │Chroma│
│  Auth  │ │(PG) │ │  AI     │ │ RAG  │
└────────┘ └─────┘ └─────────┘ └──────┘
```

### 데이터 플로우
```
사용자 입력 → Next.js → NestJS API
                           ↓
                    게임 로직 처리
                           ↓
                    ┌──────┴──────┐
                    ↓             ↓
                DB 저장      AI 요청
                    ↓             ↓
                상태 업데이트  텍스트 생성
                    ↓             ↓
                    └──────┬──────┘
                           ↓
                   응답 반환 → 화면 표시
```

---

## 데이터베이스 설계

### ERD 개요
```
users (1) ──→ (N) characters (1) ──→ (N) inventory
                    │                        
                    ├──→ (N) battle_logs
                    │
                    ├──→ (N) conversations
                    │
                    └──→ (N) save_states
```

### 테이블 상세 스키마

#### users
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  username VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  last_login TIMESTAMP DEFAULT NOW(),
  CONSTRAINT users_username_check CHECK (length(username) >= 2),
  CONSTRAINT users_email_check CHECK (email ~* '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$')
);

CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_email ON users(email);
```

#### characters
```sql
CREATE TABLE characters (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name VARCHAR(50) NOT NULL,
  class VARCHAR(20) NOT NULL CHECK (class IN ('warrior', 'mage', 'thief')),
  level INT DEFAULT 1 CHECK (level >= 1 AND level <= 100),
  exp INT DEFAULT 0 CHECK (exp >= 0),
  hp INT NOT NULL CHECK (hp >= 0),
  max_hp INT NOT NULL CHECK (max_hp > 0),
  atk INT NOT NULL CHECK (atk > 0),
  def INT NOT NULL CHECK (def >= 0),
  current_floor INT DEFAULT 1 CHECK (current_floor >= 1),
  gold INT DEFAULT 0 CHECK (gold >= 0),
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  CONSTRAINT characters_hp_check CHECK (hp <= max_hp)
);

CREATE INDEX idx_characters_user_id ON characters(user_id);
CREATE INDEX idx_characters_is_active ON characters(is_active);
```

#### inventory
```sql
CREATE TABLE inventory (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  character_id UUID NOT NULL REFERENCES characters(id) ON DELETE CASCADE,
  item_name VARCHAR(100) NOT NULL,
  item_type VARCHAR(50) NOT NULL CHECK (
    item_type IN ('potion', 'weapon', 'armor', 'accessory', 'material')
  ),
  quantity INT DEFAULT 1 CHECK (quantity > 0),
  stats_bonus JSONB DEFAULT '{}',
  is_equipped BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW(),
  CONSTRAINT inventory_unique_equipped CHECK (
    NOT is_equipped OR 
    NOT EXISTS (
      SELECT 1 FROM inventory i2 
      WHERE i2.character_id = character_id 
      AND i2.item_type = item_type 
      AND i2.is_equipped = true 
      AND i2.id != id
    )
  )
);

CREATE INDEX idx_inventory_character_id ON inventory(character_id);
CREATE INDEX idx_inventory_item_type ON inventory(item_type);
```

#### battle_logs
```sql
CREATE TABLE battle_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  character_id UUID NOT NULL REFERENCES characters(id) ON DELETE CASCADE,
  monster_name VARCHAR(100) NOT NULL,
  monster_level INT NOT NULL,
  result VARCHAR(20) NOT NULL CHECK (result IN ('victory', 'defeat', 'escape')),
  damage_dealt INT DEFAULT 0,
  damage_taken INT DEFAULT 0,
  exp_gained INT DEFAULT 0,
  gold_gained INT DEFAULT 0,
  items_gained JSONB DEFAULT '[]',
  battle_duration INT, -- 전투 턴 수
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_battle_logs_character_id ON battle_logs(character_id);
CREATE INDEX idx_battle_logs_created_at ON battle_logs(created_at);
```

#### conversations
```sql
CREATE TABLE conversations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  character_id UUID NOT NULL REFERENCES characters(id) ON DELETE CASCADE,
  role VARCHAR(20) NOT NULL CHECK (role IN ('user', 'assistant', 'system')),
  content TEXT NOT NULL,
  metadata JSONB DEFAULT '{}', -- 전투 데이터, 선택지 등
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_conversations_character_id ON conversations(character_id);
CREATE INDEX idx_conversations_created_at ON conversations(created_at);
```

#### save_states
```sql
CREATE TABLE save_states (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  character_id UUID NOT NULL REFERENCES characters(id) ON DELETE CASCADE,
  slot_number INT NOT NULL CHECK (slot_number BETWEEN 1 AND 5),
  save_name VARCHAR(100),
  game_state JSONB NOT NULL, -- 전체 게임 상태 스냅샷
  screenshot_url TEXT, -- 저장 시점 스크린샷 (선택사항)
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  CONSTRAINT save_states_unique_slot UNIQUE (character_id, slot_number)
);

CREATE INDEX idx_save_states_character_id ON save_states(character_id);
```

---

## API 명세

### 인증 API

#### POST /auth/register
회원가입

**Request**
```json
{
  "email": "user@example.com",
  "password": "password123",
  "username": "player1"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "userId": "uuid",
    "username": "player1",
    "email": "user@example.com"
  }
}
```

#### POST /auth/login
로그인

**Request**
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "accessToken": "jwt_token",
    "refreshToken": "refresh_token",
    "user": {
      "id": "uuid",
      "username": "player1",
      "email": "user@example.com"
    }
  }
}
```

**Response 401** (인증 실패)
```json
{
  "success": false,
  "error": "Invalid email or password"
}
```

#### POST /auth/logout
로그아웃

**Headers**
```
Authorization: Bearer {accessToken}
```

**Response 200**
```json
{
  "success": true,
  "message": "Logged out successfully"
}
```

#### POST /auth/refresh
토큰 갱신

**Request**
```json
{
  "refreshToken": "refresh_token_string"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "accessToken": "new_jwt_token",
    "refreshToken": "new_refresh_token"
  }
}
```

#### PATCH /auth/password
비밀번호 변경

**Headers**
```
Authorization: Bearer {accessToken}
```

**Request**
```json
{
  "currentPassword": "old_password",
  "newPassword": "new_password"
}
```

**Response 200**
```json
{
  "success": true,
  "message": "Password updated successfully"
}
```

---

### 캐릭터 API

#### POST /characters
캐릭터 생성

**Headers**
```
Authorization: Bearer {accessToken}
```

**Request**
```json
{
  "name": "용사123",
  "class": "warrior"
}
```

**Response 201**
```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "name": "용사123",
    "class": "warrior",
    "level": 1,
    "exp": 0,
    "hp": 120,
    "maxHp": 120,
    "atk": 15,
    "def": 12,
    "currentFloor": 1,
    "gold": 0
  }
}
```

#### GET /characters/:id
캐릭터 조회

**Response 200**
```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "name": "용사123",
    "class": "warrior",
    "level": 5,
    "exp": 450,
    "hp": 85,
    "maxHp": 150,
    "atk": 23,
    "def": 18,
    "currentFloor": 4,
    "gold": 350
  }
}
```

#### GET /characters/:id/stats
캐릭터 상세 스탯

**Response 200**
```json
{
  "success": true,
  "data": {
    "character": { /* 캐릭터 기본 정보 */ },
    "expToNextLevel": 50,
    "totalMonstersDefeated": 23,
    "totalGoldEarned": 1250,
    "playTime": "2h 30m",
    "achievements": []
  }
}
```

---

### 게임플레이 API

#### POST /game/chat
AI와 대화 (메인 게임 로직)

**Request**
```json
{
  "characterId": "uuid",
  "message": "앞으로 간다",
  "context": {
    "currentFloor": 3,
    "lastAction": "defeated_goblin"
  }
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "response": "복도를 따라 걷자 횃불이 깜빡이는 방이 나타났습니다.\n왼쪽 문에서 으르렁거리는 소리가 들립니다.",
    "gameState": {
      "type": "exploration",
      "options": ["문을 연다", "지나간다", "귀를 기울인다"]
    },
    "characterUpdate": null
  }
}
```

#### POST /game/action
특정 액션 실행

**Request**
```json
{
  "characterId": "uuid",
  "action": "attack",
  "target": "goblin",
  "context": {}
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "result": "hit",
    "damage": 18,
    "criticalHit": false,
    "counterAttack": {
      "damage": 5,
      "hit": true
    },
    "characterState": {
      "hp": 80,
      "maxHp": 120
    },
    "monsterState": {
      "hp": 12,
      "maxHp": 30,
      "isDefeated": false
    },
    "aiNarration": "당신의 검이 고블린을 베었습니다! 고블린이 비명을 지르며 반격합니다!"
  }
}
```

#### POST /game/battle/start
전투 시작

**Request**
```json
{
  "characterId": "uuid",
  "monsterType": "goblin"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "battleId": "uuid",
    "monster": {
      "name": "고블린 전사",
      "level": 5,
      "hp": 30,
      "maxHp": 30,
      "atk": 10,
      "def": 5
    },
    "aiNarration": "고블린 전사가 칼을 들고 덤벼듭니다!"
  }
}
```

#### POST /game/battle/attack
공격

**Request**
```json
{
  "characterId": "uuid",
  "battleId": "uuid"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "playerTurn": {
      "damage": 18,
      "critical": false,
      "hit": true
    },
    "monsterTurn": {
      "damage": 5,
      "critical": false,
      "hit": true
    },
    "battleState": {
      "playerHp": 80,
      "monsterHp": 12,
      "turn": 2,
      "isOver": false
    },
    "aiNarration": "당신의 검이 고블린을 관통했습니다! (데미지: 18)\n고블린이 반격합니다! (데미지: 5)"
  }
}
```

#### POST /game/battle/defend
방어

**Request**
```json
{
  "characterId": "uuid",
  "battleId": "uuid"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "playerTurn": {
      "defending": true,
      "damageReduction": 50
    },
    "monsterTurn": {
      "damage": 3,
      "originalDamage": 6,
      "hit": true
    },
    "battleState": {
      "playerHp": 82,
      "monsterHp": 12,
      "turn": 3,
      "isOver": false
    },
    "aiNarration": "방어 자세를 취합니다. 고블린의 공격을 막아냈습니다!"
  }
}
```

#### POST /game/battle/escape
도망

**Request**
```json
{
  "characterId": "uuid",
  "battleId": "uuid"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "escaped": true,
    "damage": 2,
    "battleState": {
      "playerHp": 78,
      "isOver": true,
      "result": "escaped"
    },
    "aiNarration": "황급히 도망쳤습니다. 고블린의 공격을 약간 맞았습니다."
  }
}
```

---

### 저장/불러오기 API

#### POST /game/save
게임 저장

**Request**
```json
{
  "characterId": "uuid",
  "slotNumber": 1,
  "saveName": "던전 3층 진행중"
}
```

**Response 201**
```json
{
  "success": true,
  "data": {
    "saveId": "uuid",
    "slotNumber": 1,
    "saveName": "던전 3층 진행중",
    "gameState": {
      "character": { /* 전체 캐릭터 정보 */ },
      "inventory": [ /* 인벤토리 */ ],
      "currentBattle": null,
      "conversationHistory": [ /* 최근 10개 대화 */ ]
    },
    "savedAt": "2024-12-16T10:30:00Z"
  }
}
```

#### GET /game/saves/:characterId
저장 목록 조회

**Response 200**
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "slotNumber": 1,
      "saveName": "던전 3층 진행중",
      "preview": {
        "level": 5,
        "floor": 3,
        "hp": "80/120"
      },
      "savedAt": "2024-12-16T10:30:00Z"
    },
    {
      "id": "uuid",
      "slotNumber": 2,
      "saveName": "보스 전투 직전",
      "preview": {
        "level": 10,
        "floor": 10,
        "hp": "150/180"
      },
      "savedAt": "2024-12-15T18:20:00Z"
    }
  ]
}
```

#### POST /game/load
게임 불러오기

**Request**
```json
{
  "saveId": "uuid"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "character": { /* 복원된 캐릭터 정보 */ },
    "inventory": [ /* 복원된 인벤토리 */ ],
    "conversationHistory": [ /* 복원된 대화 */ ],
    "message": "게임을 불러왔습니다."
  }
}
```

#### DELETE /game/saves/:saveId
저장 데이터 삭제

**Response 200**
```json
{
  "success": true,
  "message": "저장 데이터가 삭제되었습니다."
}
```

---

### 인벤토리 API

#### GET /inventory/:characterId
인벤토리 조회

**Response 200**
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "itemName": "체력 포션",
      "itemType": "potion",
      "quantity": 3,
      "statsBonus": {
        "hpRestore": 50
      }
    },
    {
      "id": "uuid",
      "itemName": "강철 검",
      "itemType": "weapon",
      "quantity": 1,
      "statsBonus": {
        "atk": 5
      },
      "isEquipped": true
    }
  ]
}
```

#### POST /inventory/use
아이템 사용

**Request**
```json
{
  "characterId": "uuid",
  "itemId": "uuid"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "itemUsed": "체력 포션",
    "effect": {
      "hpRestored": 50,
      "newHp": 120,
      "maxHp": 120
    },
    "remainingQuantity": 2,
    "message": "체력 포션을 사용했습니다. HP가 50 회복되었습니다!"
  }
}
```

#### POST /inventory/equip
장비 착용

**Request**
```json
{
  "characterId": "uuid",
  "itemId": "uuid"
}
```

**Response 200**
```json
{
  "success": true,
  "data": {
    "equipped": "강철 검",
    "statsChange": {
      "atk": "+5"
    },
    "unequipped": "낡은 검",
    "message": "강철 검을 착용했습니다."
  }
}
```

---

### 기록 API

#### GET /battles/:characterId
전투 기록 조회

**Query Parameters**
- limit: number (기본값: 10)
- offset: number (기본값: 0)

**Response 200**
```json
{
  "success": true,
  "data": {
    "battles": [
      {
        "id": "uuid",
        "monsterName": "고블린 전사",
        "monsterLevel": 5,
        "result": "victory",
        "damageDealt": 45,
        "damageTaken": 15,
        "expGained": 50,
        "goldGained": 25,
        "itemsGained": ["체력 포션"],
        "battleDuration": 3,
        "createdAt": "2024-12-16T10:25:00Z"
      }
    ],
    "total": 23,
    "page": 1,
    "totalPages": 3
  }
}
```

#### GET /conversations/:characterId
대화 기록 조회

**Query Parameters**
- limit: number (기본값: 20)
- offset: number (기본값: 0)

**Response 200**
```json
{
  "success": true,
  "data": {
    "conversations": [
      {
        "id": "uuid",
        "role": "user",
        "content": "앞으로 간다",
        "createdAt": "2024-12-16T10:20:00Z"
      },
      {
        "id": "uuid",
        "role": "assistant",
        "content": "복도를 따라 걷자...",
        "createdAt": "2024-12-16T10:20:02Z"
      }
    ],
    "total": 145
  }
}
```

---

## 게임 시스템 설계

### 직업 시스템

#### 초기 스탯 테이블
| 직업 | HP | ATK | DEF | 특성 |
|------|-----|-----|-----|------|
| 용사 (warrior) | 120 | 15 | 12 | 균형잡힌 스탯 |
| 마법사 (mage) | 80 | 20 | 8 | 높은 공격력 |
| 도둑 (thief) | 100 | 18 | 10 | 높은 회피율 |

#### 레벨업 스탯 증가량
| 직업 | HP | ATK | DEF |
|------|-----|-----|-----|
| 용사 | +10 | +2 | +2 |
| 마법사 | +6 | +3 | +1 |
| 도둑 | +8 | +2.5 | +1.5 |

---

### 전투 시스템

#### 데미지 계산 공식
```
기본 데미지 = ATK - (DEF / 2)
최소 데미지 = 1
크리티컬 데미지 = 기본 데미지 × 1.5

크리티컬 확률:
- 용사: 10%
- 마법사: 10%
- 도둑: 20%

회피 확률:
- 용사: 5%
- 마법사: 5%
- 도둑: 15%
```

#### 전투 턴 순서
```
1. 플레이어 행동 선택 (공격/방어/아이템/도망)
2. 플레이어 행동 실행
3. 몬스터가 생존해 있으면 몬스터 행동
4. 전투 결과 판정
   - 몬스터 HP = 0 → 승리
   - 플레이어 HP = 0 → 패배
   - 도망 성공 → 전투 종료
5. 다음 턴으로 이동 또는 전투 종료
```

#### 방어 메커니즘
```
방어 시:
- 받는 데미지 50% 감소
- 다음 턴까지 방어 상태 유지
- 반격 불가능
```

#### 도망 메커니즘
```
도망 성공률:
- 기본: 50%
- 도둑: 70%
- 보스전: 0% (도망 불가)

도망 실패 시:
- 몬스터의 공격 1회 받음
- 다시 도망 시도 가능
```

---

### 레벨 시스템

#### 경험치 테이블
```
레벨 n에서 레벨 n+1로 가는데 필요한 경험치:
필요 경험치 = 현재레벨 × 100

예시:
Lv.1 → Lv.2: 100 EXP
Lv.2 → Lv.3: 200 EXP
Lv.5 → Lv.6: 500 EXP
```

#### 레벨업 보상
```
1. 스탯 자동 증가 (직업별 차등)
2. HP 완전 회복
3. 레벨업 알림 표시
```

---

### 몬스터 시스템

#### 몬스터 생성 공식
```
몬스터 레벨 = 플레이어 레벨 ± 2 (랜덤)
몬스터 HP = 플레이어 레벨 × 15 × 난이도 배율
몬스터 ATK = 플레이어 레벨 × 2 × 난이도 배율
몬스터 DEF = 플레이어 레벨 × 1 × 난이도 배율

난이도 배율:
- 초급 (1-10층): 0.8
- 중급 (11-20층): 1.0
- 고급 (21-30층): 1.2
- 보스: 2.0
```

#### 몬스터 종류 (층별)
```
초급 던전 (1-10층):
- 슬라임 (Lv.1-3)
- 고블린 (Lv.3-6)
- 늑대 (Lv.6-9)
- 오크 (Lv.9-10, 보스)

중급 던전 (11-20층):
- 스켈레톤 (Lv.11-14)
- 좀비 (Lv.14-17)
- 뱀파이어 (Lv.17-19)
- 리치 (Lv.20, 보스)

고급 던전 (21-30층):
- 드레이크 (Lv.21-24)
- 데몬 (Lv.24-27)
- 드래곤 (Lv.27-29)
- 마왕 (Lv.30, 최종 보스)
```

#### 경험치 및 보상
```
일반 몬스터:
- 경험치: 몬스터 레벨 × 10
- 골드: 몬스터 레벨 × 5
- 아이템 드롭률: 30%

보스 몬스터:
- 경험치: 몬스터 레벨 × 50
- 골드: 몬스터 레벨 × 25
- 아이템 드롭률: 100%
```

---

### 아이템 시스템

#### 아이템 종류

**소비 아이템**
```
체력 포션:
- 효과: HP 50 회복
- 가격: 50 골드
- 드롭률: 30%

마나 포션:
- 효과: 강력한 공격 1회 (데미지 2배)
- 가격: 100 골드
- 드롭률: 15%

부활의 깃털:
- 효과: 사망 시 자동 부활 (HP 50%)
- 가격: 500 골드
- 드롭률: 5%
```

**장비 아이템 (무기)**
```
낡은 검 (Lv.1):
- ATK +3
- 초기 지급

강철 검 (Lv.5):
- ATK +8
- 가격: 200 골드

명검 (Lv.10):
- ATK +15
- 가격: 500 골드

전설의 검 (Lv.20):
- ATK +30
- 보스 드롭 전용
```

**장비 아이템 (방어구)**
```
가죽 갑옷 (Lv.1):
- DEF +2
- 초기 지급

철갑옷 (Lv.5):
- DEF +6
- 가격: 250 골드

플레이트 아머 (Lv.10):
- DEF +12
- 가격: 600 골드

용비늘 갑옷 (Lv.20):
- DEF +25
- 보스 드롭 전용
```

#### 아이템 드롭 테이블
```
몬스터 처치 시:
1. 드롭 여부 판정 (30%)
2. 드롭 시 아이템 종류 결정:
   - 체력 포션: 50%
   - 마나 포션: 25%
   - 무기: 15%
   - 방어구: 10%
3. 레벨에 맞는 아이템 선택
```

---

### 저장/불러오기 시스템

#### 저장 슬롯
```
- 캐릭터당 5개 슬롯 제공
- 각 슬롯에 저장 이름 설정 가능
- 자동 저장 기능 (슬롯 0번, 덮어쓰기)
```

#### 저장되는 데이터
```json
{
  "saveVersion": "1.0",
  "savedAt": "2024-12-16T10:30:00Z",
  "character": {
    "id": "uuid",
    "name": "용사123",
    "class": "warrior",
    "level": 5,
    "exp": 450,
    "hp": 85,
    "maxHp": 150,
    "atk": 23,
    "def": 18,
    "currentFloor": 4,
    "gold": 350
  },
  "inventory": [
    {
      "itemName": "체력 포션",
      "quantity": 3
    }
  ],
  "equippedItems": {
    "weapon": "강철 검",
    "armor": "철갑옷"
  },
  "currentBattle": null,
  "conversationHistory": [
    {
      "role": "user",
      "content": "앞으로 간다",
      "timestamp": "2024-12-16T10:25:00Z"
    }
  ],
  "gameFlags": {
    "tutorialCompleted": true,
    "bossesDefeated": ["오크"]
  }
}
```

#### 자동 저장 트리거
```
- 전투 승리 시
- 레벨업 시
- 층 이동 시
- 5분마다 (idle 상태 제외)
```

#### 불러오기 프로세스
```
1. 저장 슬롯 선택
2. 저장 데이터 검증
3. 캐릭터 상태 복원
4. 인벤토리 복원
5. 대화 히스토리 복원 (최근 10개)
6. 게임 재개
```

---

## AI 통합 가이드

### Qwen 2.5 7B 설정

#### 모델 서빙 (vLLM)
```bash
# vLLM 서버 실행
python -m vllm.entrypoints.openai.api_server \
  --model Qwen/Qwen2.5-7B-Instruct \
  --host 0.0.0.0 \
  --port 8001 \
  --dtype auto \
  --max-model-len 4096
```

#### 추론 파라미터
```python
generation_config = {
    "max_tokens": 150,        # 2-3줄 응답
    "temperature": 0.7,       # 적당한 창의성
    "top_p": 0.9,
    "top_k": 50,
    "repetition_penalty": 1.1,
    "stop": ["\n\n", "###"]  # 중지 토큰
}
```

---

### AI 역할 정의

#### AI가 하는 일
1. **던전 탐험 묘사**: 분위기, 환경, 상황 표현
2. **몬스터 등장 묘사**: 외형, 분위기 표현
3. **전투 장면 묘사**: 공격/방어 행동을 생생하게 표현
4. **NPC 대화**: 간단한 대화 응답
5. **힌트 제공**: 막혔을 때 방향 제시

#### AI가 하지 않는 일
1. **전투 계산**: 데미지, HP 등은 백엔드 처리
2. **게임 규칙 결정**: 레벨업, 경험치는 서버 로직
3. **데이터 저장**: DB 작업은 별도 처리
4. **밸런싱**: 난이도는 사전 정의된 값 사용

---

### 시스템 프롬프트

#### 기본 시스템 프롬프트
```
당신은 판타지 텍스트 MUD 게임의 던전 마스터입니다.

역할:
- 플레이어의 던전 탐험을 생생하게 묘사합니다
- 몬스터 등장 상황을 긴장감 있게 표현합니다
- 전투 장면을 역동적으로 묘사합니다

규칙:
- 항상 2-3줄 이내로 간결하게 답변하세요
- 몰입감을 주는 구체적인 표현을 사용하세요
- 게임 데이터(HP, 데미지 등)는 제공받은 값만 사용하세요
- 한국어로 답변하세요

금지 사항:
- 전투 결과를 임의로 결정하지 마세요
- 아이템이나 경험치를 임의로 생성하지 마세요
- 게임 규칙을 변경하지 마세요
- 4줄 이상 장황하게 설명하지 마세요
```

---

### 상황별 프롬프트 템플릿

#### 던전 탐험
```
상황: 플레이어가 "{action}"을 수행했습니다.
현재 위치: {floor}층
주변 환경: {environment}

위 상황을 2줄로 생생하게 묘사하고, 다음에 무엇을 할 수 있는지 간단히 제시하세요.
```

**예시 입력**
```
상황: 플레이어가 "앞으로 간다"를 수행했습니다.
현재 위치: 3층
주변 환경: 어두운 복도
```

**예시 출력**
```
횃불이 깜빡이는 복도를 따라가자 갈림길이 나타났습니다.
왼쪽에서 으르렁거리는 소리가, 오른쪽에서는 물소리가 들립니다.
```

---

#### 몬스터 조우
```
상황: {monster_name} Lv.{level}이(가) 등장했습니다.
외형: {description}
분위기: {mood}

몬스터 등장 장면을 긴장감 있게 2줄로 묘사하세요.
```

**예시 입력**
```
상황: 고블린 전사 Lv.5가 등장했습니다.
외형: 녹색 피부, 날카로운 이빨, 녹슨 검
분위기: 적대적
```

**예시 출력**
```
문을 열자 녹색 피부의 고블린이 날카로운 이빨을 드러내며 으르렁거립니다.
녹슨 검을 휘두르며 당신을 향해 돌진합니다!
```

---

#### 전투 묘사
```
전투 상황:
- 플레이어 행동: {action}
- 플레이어 데미지: {player_damage}
- 크리티컬 여부: {critical}
- 몬스터: {monster_name}
- 몬스터 데미지: {monster_damage}
- 플레이어 HP: {player_hp}/{player_max_hp}
- 몬스터 HP: {monster_hp}/{monster_max_hp}

위 전투 결과를 2줄로 생생하게 묘사하세요.
첫 줄: 플레이어의 공격 묘사
둘째 줄: 몬스터의 반응 또는 반격 묘사
```

**예시 입력 (공격)**
```
전투 상황:
- 플레이어 행동: attack
- 플레이어 데미지: 18
- 크리티컬 여부: false
- 몬스터: 고블린 전사
- 몬스터 데미지: 5
- 플레이어 HP: 80/120
- 몬스터 HP: 12/30
```

**예시 출력**
```
당신의 검이 고블린의 어깨를 베었습니다! (데미지: 18)
고블린이 비명을 지르며 반격합니다! (데미지: 5)
```

**예시 입력 (방어)**
```
전투 상황:
- 플레이어 행동: defend
- 플레이어 데미지: 0
- 몬스터: 오크
- 몬스터 데미지: 3 (원래 6, 방어로 50% 감소)
- 플레이어 HP: 82/120
```

**예시 출력**
```
방어 자세를 취하고 오크의 도끼 공격을 방패로 막아냅니다!
충격이 전해지지만 피해를 최소화했습니다. (데미지: 3)
```

---

#### 레벨업
```
상황: 플레이어가 레벨업했습니다!
레벨: {old_level} → {new_level}
스탯 변화: HP +{hp}, ATK +{atk}, DEF +{def}

레벨업 순간을 축하하는 2줄 메시지를 작성하세요.
```

**예시 출력**
```
빛이 온몸을 감싸며 레벨 6으로 올랐습니다!
최대 HP가 10 증가하고, 공격력과 방어력이 각각 2씩 상승했습니다!
```

---

#### 아이템 획득
```
상황: 전투 승리 후 아이템을 획득했습니다.
아이템: {item_name} x{quantity}
아이템 설명: {description}

아이템 획득을 1줄로 표현하세요.
```

**예시 출력**
```
고블린이 떨어뜨린 체력 포션 2개를 주웠습니다.
```

---

#### 사망
```
상황: 플레이어가 전투에서 패배했습니다.
패배 원인: {monster_name}에게 패배

게임 오버 상황을 2줄로 극적으로 묘사하세요.
```

**예시 출력**
```
{monster_name}의 마지막 일격에 당신은 쓰러집니다...
의식이 희미해지며 던전의 어둠이 당신을 삼킵니다.
```

---

### RAG 시스템

#### 임베딩 대상 데이터
```
1. 캐릭터 기본 정보
   - 이름, 직업, 레벨
   - 현재 HP, ATK, DEF
   - 현재 던전 층

2. 인벤토리
   - 보유 아이템 목록
   - 장착 중인 장비

3. 전투 기록
   - 최근 처치한 몬스터 20개
   - 전투 결과

4. 대화 히스토리
   - 최근 대화 10개
```

#### 검색 쿼리 예시
```
사용자: "내가 지금까지 잡은 몬스터가 뭐야?"
→ 검색: battle_logs 테이블의 defeated monsters
→ AI 응답: "지금까지 슬라임 5마리, 고블린 3마리, 오크 1마리를 처치했습니다."

사용자: "내 인벤토리 뭐있어?"
→ 검색: inventory 테이블
→ AI 응답: "체력 포션 3개, 마나 포션 1개, 강철 검(장착 중), 철갑옷(장착 중)을 보유하고 있습니다."

사용자: "지금 몇 층이야?"
→ 검색: character의 current_floor
→ AI 응답: "현재 던전 4층에 있습니다."
```

#### RAG 프로세스
```
1. 사용자 질문 수신
2. 질문을 임베딩으로 변환
3. Vector DB에서 유사도 Top-3 검색
4. 검색된 컨텍스트를 AI 프롬프트에 추가
5. AI가 자연스럽게 답변 생성
6. 사용자에게 응답 반환
```

---

## 화면 설계

### 공통 디자인 가이드

#### 색상 팔레트
```css
/* 일반 UI 영역 */
--primary: #3b82f6;        /* 파란색 */
--secondary: #64748b;      /* 회색 */
--success: #10b981;        /* 녹색 */
--danger: #ef4444;         /* 빨간색 */
--warning: #f59e0b;        /* 주황색 */
--background: #f1f5f9;     /* 밝은 회색 */
--text: #1e293b;           /* 진한 회색 */

/* CLI 영역 (채팅 영역만) */
--cli-bg: #0a0e14;         /* 다크 블루 블랙 */
--cli-text: #00ff00;       /* 그린 터미널 */
--cli-text-dim: #33ff33;   /* 밝은 그린 */
--cli-danger: #ff6b6b;     /* 빨강 (HP, 위험) */
--cli-warning: #ffd93d;    /* 노랑 (경고) */
--cli-border: #2a3f5f;     /* 테두리 */
```

#### 타이포그래피
```css
/* 일반 UI */
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
font-size: 16px;

/* CLI 영역 */
font-family: 'JetBrains Mono', 'Courier New', 'Consolas', monospace;
font-size: 14px;
line-height: 1.6;
```

---

### 화면 1: 채팅 화면

#### 레이아웃 구조
```
┌─────────────────────────────────┐
│  Header (일반 디자인)            │  50px
├─────────────────────────────────┤
│                                 │
│                                 │
│  Chat Area (CLI 스타일)          │  calc(100vh - 130px)
│                                 │
│                                 │
├─────────────────────────────────┤
│  Input Area (일반 디자인)         │  80px
└─────────────────────────────────┘
```

#### Header 영역
```
구성 요소:
- 좌측: 캐릭터 이름 + 레벨 표시
- 우측: 햄버거 메뉴 버튼

예시:
┌─────────────────────────────────┐
│ 용사123 Lv.5          ☰        │
└─────────────────────────────────┘

햄버거 메뉴 펼쳐질 때:
- 마이페이지
- 게임 저장
- 게임 불러오기
- 로그아웃
```

#### Chat Area 영역 (CLI 스타일)

**배경 및 기본 스타일**
```
- 배경: 검은색 (#0a0e14)
- 패딩: 16px
- 오버플로우: 스크롤 (자동)
- 스크롤 위치: 항상 최하단
```

**메시지 타입별 스타일**

1. **시스템 메시지**
```
색상: 연두색 (#33ff33)
형식: [SYSTEM] 메시지 내용

예시:
[SYSTEM] 던전 3층에 도착했습니다.
```

2. **AI 메시지 (던전 마스터)**
```
색상: 녹색 (#00ff00)
형식: > 메시지 내용

예시:
> 복도를 따라 걷자 횃불이 깜빡이는 방이 나타났습니다.
> 왼쪽 문에서 으르렁거리는 소리가 들립니다.
```

3. **사용자 입력**
```
색상: 흰색 (#ffffff)
형식: $ 사용자 입력

예시:
$ 앞으로 간다
```

4. **전투 로그**
```
색상: 빨간색 (#ff6b6b)
형식: [BATTLE] 전투 내용

예시:
[BATTLE] 당신의 공격! 데미지: 18
[BATTLE] 고블린의 반격! 데미지: 5
```

5. **아이템/골드 획득**
```
색상: 노란색 (#ffd93d)
형식: [ITEM] 아이템 내용

예시:
[ITEM] 체력 포션 x2 획득!
[GOLD] 25 골드 획득!
```

**ASCII 아트 요소**

1. **HP 바**
```
HP: ████████░░ 80/120
```

계산 방식:
```
총 10칸
채워진 칸 = floor((현재HP / 최대HP) * 10)
빈 칸 = 10 - 채워진 칸

색상:
- 70% 이상: 녹색
- 40-69%: 노란색
- 40% 미만: 빨간색
```

2. **경험치 바**
```
EXP: ████████░░ 450/500
```

3. **커서 애니메이션**
```
> 당신의 차례입니다_

_ (언더스코어)가 깜빡임 (0.5초 간격)
```

4. **선택지 표시**
```
━━━━━━━━━━━━━━━━━━━━━
 무엇을 하시겠습니까?
━━━━━━━━━━━━━━━━━━━━━
1. 공격한다
2. 방어한다
3. 아이템 사용
4. 도망친다
━━━━━━━━━━━━━━━━━━━━━
```

5. **몬스터 정보**
```
┌──────────────────────┐
│  고블린 전사 Lv.5    │
│  HP: ████░░░░░░ 12/30│
│  ATK: 10  DEF: 5     │
└──────────────────────┘
```

**타이핑 애니메이션**
```
AI 메시지가 표시될 때:
- 한 글자씩 나타남
- 속도: 30ms/글자
- 사용자는 탭/클릭으로 즉시 완성 가능
```

#### Input Area 영역 (일반 디자인)

```
구성:
- 텍스트 입력창 (80% 너비)
- 전송 버튼 (20% 너비)

스타일:
- 일반적인 모바일 메신저 스타일
- 둥근 모서리
- 그림자 효과
```

---

### 화면 2: 마이페이지

#### 레이아웃 구조
```
┌─────────────────────────────────┐
│  Header                         │  50px
├─────────────────────────────────┤
│                                 │
│  Scrollable Content             │
│  ┌───────────────────────┐     │
│  │ 캐릭터 정보 카드      │     │
│  └───────────────────────┘     │
│  ┌───────────────────────┐     │
│  │ 스탯 정보 카드        │     │
│  └───────────────────────┘     │
│  ┌───────────────────────┐     │
│  │ 인벤토리 카드         │     │
│  └───────────────────────┘     │
│  ┌───────────────────────┐     │
│  │ 던전 진행도 카드       │     │
│  └───────────────────────┘     │
│  ┌───────────────────────┐     │
│  │ 전투 기록 카드         │     │
│  └───────────────────────┘     │
│                                 │
└─────────────────────────────────┘
```

#### Header
```
┌─────────────────────────────────┐
│  ← 마이페이지                   │
└─────────────────────────────────┘

좌측: 뒤로가기 버튼 (채팅 화면으로)
```

#### 캐릭터 정보 카드
```
┌─────────────────────────────────┐
│  🛡️ 용사123                     │
│  직업: 용사 | Lv.5               │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│  EXP: ████████░░ 450/500        │
│  생성일: 2024-12-16               │
└─────────────────────────────────┘

아이콘:
- 용사: 🛡️
- 마법사: 🔮
- 도둑: 🗡️
```

#### 스탯 정보 카드
```
┌─────────────────────────────────┐
│  📊 스탯 정보                     │
│                                 │
│  HP  ████████░░ 85/150          │
│  ATK ▸ 23                       │
│  DEF ▸ 18                       │
│  골드 💰 350                     │
│                                 │
│  다음 레벨까지: 50 EXP           │
└─────────────────────────────────┘
```

#### 인벤토리 카드
```
┌─────────────────────────────────┐
│  🎒 인벤토리                      │
│                                 │
│  [소비 아이템]                   │
│  🧪 체력 포션 x3     [사용]     │
│  🧪 마나 포션 x1     [사용]     │
│                                 │
│  [장착 아이템]                   │
│  ⚔️ 강철 검         [장착중]    │
│  🛡️ 철갑옷          [장착중]    │
│                                 │
│  [기타]                          │
│  📜 낡은 지도 x1                 │
└─────────────────────────────────┘

버튼:
- [사용]: 아이템 즉시 사용
- [장착중]: 현재 장착 상태 표시
```

#### 던전 진행도 카드
```
┌─────────────────────────────────┐
│  🏰 던전 진행도                   │
│                                 │
│  현재 층: 4층 / 30층              │
│  ████░░░░░░░░░░░░░░░░░░░░░░ 13% │
│                                 │
│  처치한 몬스터: 23마리             │
│  획득한 골드: 1,250              │
│  플레이 시간: 2시간 30분          │
└─────────────────────────────────┘
```

#### 전투 기록 카드
```
┌─────────────────────────────────┐
│  ⚔️ 최근 전투 기록               │
│                                 │
│  고블린 전사 Lv.5    ✅ 승리    │
│  EXP +50  💰 +25                │
│  10분 전                         │
│  ─────────────────────────────  │
│  슬라임 Lv.3         ✅ 승리    │
│  EXP +30  💰 +15                │
│  25분 전                         │
│  ─────────────────────────────  │
│  오크 Lv.8           ❌ 패배    │
│  EXP +0                         │
│  1시간 전                        │
│                                 │
│  [더보기]                        │
└─────────────────────────────────┘

아이콘:
- ✅: 승리
- ❌: 패배
- 🏃: 도망
```

---

### 모달 및 다이얼로그

#### 저장 모달
```
┌─────────────────────────────────┐
│  💾 게임 저장                    │
│                                 │
│  슬롯 선택:                      │
│                                 │
│  [ Slot 1: 던전 3층 진행중 ]     │
│  Lv.5 | HP 80/120 | 2시간 전    │
│                                 │
│  [ Slot 2: 비어있음 ]            │
│                                 │
│  [ Slot 3: 보스 전투 직전 ]      │
│  Lv.10 | HP 150/180 | 1일 전    │
│                                 │
│  저장 이름:                      │
│  [____________________]         │
│                                 │
│  [취소]            [저장]       │
└─────────────────────────────────┘
```

#### 불러오기 모달
```
┌─────────────────────────────────┐
│  📂 게임 불러오기                │
│                                 │
│  [ Slot 1: 던전 3층 진행중 ]     │
│  Lv.5 | HP 80/120              │
│  저장 시간: 2024-12-16 10:30    │
│  [불러오기] [삭제]               │
│                                 │
│  [ Slot 2: 비어있음 ]            │
│                                 │
│  [ Slot 3: 보스 전투 직전 ]      │
│  Lv.10 | HP 150/180            │
│  저장 시간: 2024-12-15 18:20    │
│  [불러오기] [삭제]               │
│                                 │
│  [취소]                          │
└─────────────────────────────────┘
```

#### 게임 오버 모달
```
┌─────────────────────────────────┐
│                                 │
│       💀 GAME OVER 💀           │
│                                 │
│  {monster_name}에게 패배했습니다 │
│                                 │
│  최종 레벨: 5                    │
│  처치한 몬스터: 23마리            │
│  도달한 층: 4층                  │
│                                 │
│  [게임 저장으로 되돌아가기]       │
│  [처음부터 다시 시작]             │
│                                 │
└─────────────────────────────────┘
```

#### 레벨업 알림
```
┌─────────────────────────────────┐
│         ✨ LEVEL UP! ✨         │
│                                 │
│      Lv.5 → Lv.6               │
│                                 │
│  HP  +10  (150 → 160)          │
│  ATK +2   (23 → 25)            │
│  DEF +2   (18 → 20)            │
│                                 │
│  HP가 완전히 회복되었습니다!     │
│                                 │
│  [확인]                          │
└─────────────────────────────────┘
```

---

### 반응형 디자인

#### 모바일 (320px - 768px)
```
- 기본 타겟 화면
- 모든 요소 세로 배치
- 카드 너비: 100% (양옆 16px 여백)
- 폰트 크기: 기본값 사용
```

#### 태블릿 (768px - 1024px)
```
- 카드 너비: 최대 600px (중앙 정렬)
- 여백 증가
```

#### 데스크톱 (1024px 이상)
```
- 카드 너비: 최대 800px (중앙 정렬)
- 양옆 여백 증가
- 메시지: "모바일에서 최적화된 경험을 제공합니다"
```

---

## 개발 일정

### Week 1: 핵심 기능 구현

#### Day 1: 프로젝트 셋업
- [ ] Next.js 프로젝트 생성
- [ ] NestJS 프로젝트 생성
- [ ] PostgreSQL Docker 컨테이너 실행
- [ ] Prisma 스키마 작성 및 마이그레이션
- [ ] Git 저장소 설정

#### Day 2: 인증 시스템
- [ ] JWT 인증 전략 구현
- [ ] bcrypt 비밀번호 해싱 구현
- [ ] 회원가입 API 구현 (이메일/비밀번호)
- [ ] 로그인 API 구현
- [ ] Refresh Token 로직
- [ ] 프론트엔드 로그인/회원가입 화면

#### Day 3: AI 통합
- [ ] Qwen 2.5 7B vLLM 서버 설정
- [ ] AI API 래퍼 서비스 작성
- [ ] 기본 프롬프트 템플릿 작성
- [ ] AI 응답 테스트

#### Day 4: 캐릭터 시스템
- [ ] 캐릭터 생성 API
- [ ] 직업별 초기 스탯 설정
- [ ] 캐릭터 조회 API
- [ ] 프론트엔드 캐릭터 생성 화면

#### Day 5: 게임 로직 (전투)
- [ ] 전투 시작 API
- [ ] 공격/방어/도망 로직
- [ ] 데미지 계산 시스템
- [ ] 전투 결과 처리

#### Day 6: 채팅 화면 UI
- [ ] CLI 스타일 컴포넌트 구현
- [ ] 메시지 송수신 기능
- [ ] 타이핑 애니메이션
- [ ] ASCII 아트 HP 바

#### Day 7: 마이페이지 UI
- [ ] 캐릭터 정보 표시
- [ ] 스탯 표시
- [ ] 인벤토리 표시
- [ ] 전투 기록 표시

---

### Week 2: 완성도 향상 및 테스트

#### Day 8: 레벨 시스템
- [ ] 경험치 획득 로직
- [ ] 레벨업 계산
- [ ] 스탯 자동 증가
- [ ] 레벨업 알림 UI

#### Day 9: 아이템 시스템
- [ ] 아이템 드롭 로직
- [ ] 인벤토리 관리 API
- [ ] 아이템 사용 기능
- [ ] 장비 착용 시스템

#### Day 10: 저장/불러오기
- [ ] 저장 API 구현
- [ ] 불러오기 API 구현
- [ ] 저장 슬롯 관리
- [ ] 자동 저장 기능
- [ ] 저장/불러오기 UI

#### Day 11: 몬스터 & 던전
- [ ] 몬스터 데이터 추가 (20종)
- [ ] 층별 몬스터 배치
- [ ] 보스 몬스터 구현
- [ ] 던전 진행도 시스템

#### Day 12: RAG 시스템
- [ ] Chroma Vector DB 설정
- [ ] 컨텍스트 임베딩
- [ ] 검색 로직 구현
- [ ] AI 프롬프트에 컨텍스트 추가

#### Day 13: 테스트 & 버그 수정
- [ ] 전투 로직 테스트
- [ ] AI 응답 품질 테스트
- [ ] 저장/불러오기 테스트
- [ ] 모바일 반응형 테스트
- [ ] 버그 수정

#### Day 14: 최종 점검 & 배포
- [ ] 성능 최적화
- [ ] Docker Compose 작성
- [ ] 배포 스크립트 작성
- [ ] 문서 최종 업데이트
- [ ] 프로덕션 배포

---

## 테스트 가이드

### 단위 테스트

#### 인증 로직 테스트
```typescript
describe('Authentication', () => {
  test('비밀번호 해싱이 정상 작동하는가', async () => {
    const password = 'password123';
    const hash = await bcrypt.hash(password, 10);
    const isMatch = await bcrypt.compare(password, hash);
    expect(isMatch).toBe(true);
  });

  test('잘못된 비밀번호는 거부되는가', async () => {
    const password = 'password123';
    const hash = await bcrypt.hash(password, 10);
    const isMatch = await bcrypt.compare('wrongpassword', hash);
    expect(isMatch).toBe(false);
  });

  test('JWT 토큰이 정상 발급되는가', () => {
    const payload = { userId: 'test-uuid', email: 'test@example.com' };
    const token = jwt.sign(payload, 'secret', { expiresIn: '1d' });
    const decoded = jwt.verify(token, 'secret');
    expect(decoded.userId).toBe('test-uuid');
  });
});

#### 전투 로직 테스트
```typescript
describe('Combat System', () => {
  test('데미지 계산이 정확한가', () => {
    const damage = calculateDamage(20, 10); // ATK 20, DEF 10
    expect(damage).toBe(15); // 20 - (10/2) = 15
  });

  test('크리티컬 데미지가 1.5배인가', () => {
    const damage = calculateCriticalDamage(20);
    expect(damage).toBe(30); // 20 * 1.5 = 30
  });

  test('최소 데미지가 1인가', () => {
    const damage = calculateDamage(5, 20); // ATK 5, DEF 20
    expect(damage).toBe(1); // 최소값 1
  });
});
```

#### 레벨업 테스트
```typescript
describe('Level Up System', () => {
  test('필요 경험치가 올바른가', () => {
    const requiredExp = getRequiredExp(5);
    expect(requiredExp).toBe(500); // 레벨 5 * 100
  });

  test('레벨업 시 스탯이 증가하는가', () => {
    const character = { class: 'warrior', hp: 120, atk: 15, def: 12 };
    const newStats = levelUp(character);
    expect(newStats.hp).toBe(130); // +10
    expect(newStats.atk).toBe(17); // +2
    expect(newStats.def).toBe(14); // +2
  });
});
```

---

### 통합 테스트

#### 전투 플로우 테스트
```typescript
describe('Battle Flow', () => {
  test('전투 시작부터 승리까지', async () => {
    // 1. 전투 시작
    const battle = await startBattle(characterId, 'goblin');
    expect(battle.monster.name).toBe('고블린 전사');

    // 2. 공격
    const attackResult = await attack(characterId, battle.id);
    expect(attackResult.battleState.monsterHp).toBeLessThan(battle.monster.maxHp);

    // 3. 승리 확인
    while (!attackResult.battleState.isOver) {
      await attack(characterId, battle.id);
    }
    
    const finalResult = await getBattleResult(battle.id);
    expect(finalResult.result).toBe('victory');
    expect(finalResult.expGained).toBeGreaterThan(0);
  });
});
```

#### 저장/불러오기 테스트
```typescript
describe('Save/Load System', () => {
  test('저장 후 불러오기가 정상 작동하는가', async () => {
    // 1. 현재 상태 저장
    const currentState = await getCharacter(characterId);
    const saveResult = await saveGame(characterId, 1, '테스트 저장');
    
    // 2. 캐릭터 상태 변경
    await updateCharacter(characterId, { hp: 50, level: 10 });
    
    // 3. 저장 데이터 불러오기
    const loadResult = await loadGame(saveResult.saveId);
    
    // 4. 복원된 상태 확인
    expect(loadResult.character.hp).toBe(currentState.hp);
    expect(loadResult.character.level).toBe(currentState.level);
  });
});
```

---

### E2E 테스트

#### 신규 플레이어 플로우
```typescript
describe('New Player Flow', () => {
  test('회원가입부터 첫 전투까지', async () => {
    // 1. 회원가입
    const registerData = {
      email: 'test@example.com',
      password: 'password123',
      username: 'player1'
    };
    const user = await register(registerData);
    expect(user.userId).toBeDefined();
    expect(user.accessToken).toBeDefined();

    // 2. 캐릭터 생성
    const character = await createCharacter(user.userId, '용사123', 'warrior');
    expect(character.level).toBe(1);
    expect(character.hp).toBe(120);

    // 3. 첫 메시지 전송
    const chatResponse = await sendMessage(character.id, '앞으로 간다');
    expect(chatResponse.response).toBeDefined();

    // 4. 첫 전투
    const battleResponse = await sendMessage(character.id, '공격한다');
    expect(battleResponse.gameState.type).toBe('battle');
  });
});
```

---

### 성능 테스트

#### AI 응답 시간 테스트
```typescript
describe('AI Performance', () => {
  test('AI 응답 시간이 2초 이내인가', async () => {
    const startTime = Date.now();
    const response = await sendMessage(characterId, '앞으로 간다');
    const endTime = Date.now();
    
    expect(endTime - startTime).toBeLessThan(2000);
  });
});
```

#### 동시 사용자 테스트
```typescript
describe('Concurrent Users', () => {
  test('10명 동시 접속 처리', async () => {
    const promises = [];
    
    for (let i = 0; i < 10; i++) {
      promises.push(sendMessage(characterIds[i], '공격한다'));
    }
    
    const results = await Promise.all(promises);
    expect(results.length).toBe(10);
    results.forEach(result => {
      expect(result.success).toBe(true);
    });
  });
});
```

---

## 배포 가이드

### Docker Compose 구성

#### docker-compose.yml
```yaml
version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: dungeonchat
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/chroma/chroma

  qwen:
    image: vllm/vllm-openai:latest
    command: >
      --model Qwen/Qwen2.5-7B-Instruct
      --host 0.0.0.0
      --port 8001
      --dtype auto
      --max-model-len 4096
    ports:
      - "8001:8001"
    volumes:
      - ./models:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://admin:password@postgres:5432/dungeonchat
      QWEN_API_URL: http://qwen:8001
      CHROMA_URL: http://chroma:8000
    ports:
      - "4000:4000"
    depends_on:
      - postgres
      - chroma
      - qwen

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  postgres_data:
  chroma_data:
```

---

### 환경 변수 설정

#### Backend (.env)
```env
# Database
DATABASE_URL=postgresql://admin:password@postgres:5432/dungeonchat

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=1d
JWT_REFRESH_SECRET=your-refresh-token-secret-key
JWT_REFRESH_EXPIRES_IN=7d

# Password Hashing
BCRYPT_SALT_ROUNDS=10

# AI
QWEN_API_URL=http://qwen:8001
QWEN_MODEL_NAME=Qwen/Qwen2.5-7B-Instruct

# Vector DB
CHROMA_URL=http://chroma:8000
CHROMA_COLLECTION=dungeonchat_context

# Server
PORT=4000
NODE_ENV=production
```

#### Frontend (.env.local)
```env
NEXT_PUBLIC_API_URL=http://localhost:4000
```

---

### 로컬 실행 방법

#### 1. 사전 준비
```bash
# Docker 설치 확인
docker --version
docker-compose --version

# Git 클론
git clone https://github.com/your-repo/dungeonchat.git
cd dungeonchat
```

#### 2. 환경 변수 설정
```bash
# Backend 환경 변수
cp backend/.env.example backend/.env
# .env 파일 수정

# Frontend 환경 변수
cp frontend/.env.example frontend/.env.local
# .env.local 파일 수정
```

#### 3. 데이터베이스 마이그레이션
```bash
cd backend
npm install
npx prisma generate
npx prisma migrate deploy
```

#### 4. Docker Compose 실행
```bash
# 전체 서비스 시작
docker-compose up -d

# 로그 확인
docker-compose logs -f

# 특정 서비스만 재시작
docker-compose restart backend
```

#### 5. 서비스 접속
```
- Frontend: http://localhost:3000
- Backend API: http://localhost:4000
- Qwen AI: http://localhost:8001
- Chroma DB: http://localhost:8000
- PostgreSQL: localhost:5432
```

---

### 프로덕션 배포 체크리스트

#### 보안
- [ ] 환경 변수 암호화
- [ ] HTTPS 설정
- [ ] CORS 설정 확인
- [ ] Rate Limiting 설정
- [ ] SQL Injection 방지 확인
- [ ] XSS 방지 확인

#### 성능
- [ ] Database 인덱스 확인
- [ ] API 응답 캐싱
- [ ] 이미지 최적화
- [ ] Bundle 크기 최적화
- [ ] CDN 설정 (선택)

#### 모니터링
- [ ] 에러 로깅 설정
- [ ] 성능 모니터링 설정
- [ ] 알림 설정 (서버 다운, 에러 급증 등)
- [ ] 백업 자동화

#### 문서
- [ ] API 문서 최신화
- [ ] README 작성
- [ ] 배포 가이드 작성
- [ ] 트러블슈팅 가이드 작성

---

## 부록

### 자주 묻는 질문 (FAQ)

#### Q1: AI 응답이 느려요
```
원인:
- GPU 메모리 부족
- 모델 로딩 시간
- 네트워크 지연

해결:
1. vLLM 파라미터 조정 (--max-model-len 줄이기)
2. 8bit 양자화 사용
3. 배치 크기 조정
4. 응답 토큰 수 제한 (max_tokens: 150)
```

#### Q2: 데이터베이스 연결 오류
```
원인:
- PostgreSQL 컨테이너 미실행
- 잘못된 DATABASE_URL
- 방화벽 차단

해결:
1. docker-compose ps로 컨테이너 상태 확인
2. .env 파일의 DATABASE_URL 확인
3. 포트 5432 열림 확인
```

#### Q3: 메모리 부족 오류
```
원인:
- Qwen 7B 모델 크기 (약 8GB)
- 동시 사용자 증가

해결:
1. GPU 메모리 8GB 이상 확보
2. 양자화 사용 (8bit, 4bit)
3. 동시 요청 제한
4. 작은 모델 사용 고려
```

---

### 트러블슈팅

#### 전투 계산 오류
```
증상: 데미지가 음수로 표시됨
원인: DEF가 ATK의 2배 이상
해결: 최소 데미지 1 보장 로직 추가
```

#### 저장 데이터 손실
```
증상: 저장한 데이터를 불러올 수 없음
원인: game_state JSONB 직렬화 오류
해결: JSON.stringify/parse 검증
```

#### AI 응답 형식 오류
```
증상: AI가 규칙을 지키지 않음
원인: 프롬프트 엔지니어링 부족
해결: 시스템 프롬프트 강화, Few-shot 예시 추가
```

---

### 용어 사전

| 용어 | 설명 |
|------|------|
| MUD | Multi-User Dungeon, 텍스트 기반 RPG |
| CLI | Command Line Interface |
| RAG | Retrieval-Augmented Generation |
| vLLM | LLM 서빙을 위한 고성능 프레임워크 |
| Qwen | Alibaba에서 개발한 LLM |
| 턴제 | 순서대로 행동하는 전투 방식 |
| ASCII 아트 | 문자로 그린 그림 |
| 임베딩 | 텍스트를 벡터로 변환 |

---

### 참고 자료

#### 기술 문서
- [Qwen 2.5 공식 문서](https://github.com/QwenLM/Qwen)
- [vLLM 문서](https://docs.vllm.ai/)
- [NestJS 공식 문서](https://docs.nestjs.com/)
- [Next.js 공식 문서](https://nextjs.org/docs)
- [Prisma 문서](https://www.prisma.io/docs)
- [Chroma DB 문서](https://docs.trychroma.com/)

#### 게임 디자인 참고
- MUD 게임 역사 및 디자인
- 던전 크롤러 게임 메커니즘
- 텍스트 어드벤처 게임 Best Practices

---

## 문서 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 1.0 | 2024-12-16 | 초안 작성 |

---

**문서 끝**
